(prefix){
    (目标编程语言：直接回答)
}

(引入){
    !(main.ts)
}

(给出问题解决方案){
    (问题描述){
        (   
            现在的问题是：
                如果用户输入npml -p，会显示help信息；
                如果用户输入npml -p 1，或者其他任意字符，才会开始同步提示词。
            我希望是用户输入npml -p，直接开始同步。
        )
    }
}